import { ConsultationPurpose } from './consultation-purpose';

describe('ConsultationPurpose', () => {
  it('should create an instance', () => {
    expect(new ConsultationPurpose()).toBeTruthy();
  });
});
